# Docker Container to Run the Quartz-enabled Traffic Controller #

To build the container run the following script:
```
$> ./build.sh
```

To run the container run the following script:
```
$> ./run.sh
```
